# Devlog — 2026-09-05（T003: Modbus RTU Frame Model）

- 补录 ADR001（用户在 T003 开始前确认：最终 UI = Qt Quick/QML；QMainWindow 仅为 scaffold，T008 切换；Modbus Core 不得依赖 QML）。
- TDD RED：先建 `tests/test_modbus_rtu_frame.cpp`（FRAME-T01~T04）+ target `modbuslens_frame_tests`，头文件未建时构建失败——`fatal error: core/protocol/ModbusRtuFrame.h: No such file or directory`（test 文件第 6 行），如实存档。
- 实现：`src/core/protocol/ModbusRtuFrame.h`（header-only）——aggregate struct（address/functionCode/data）+ defaulted `operator==` + inline `isExceptionResponse`（`functionCode & 0x80`）；设计要点：**CRC 是派生值不存入模型**（防 stale CRC），encode 现算/decode 先验属 T004；raw bytes 诊断视图未来单独挂附。
- GREEN：FRAME-T01~T04 全过（6 passed, 0 failed, 7ms）；全项目 ctest 3/3（smoke/crc/frame）；clean 全量重建零警告；CRC 既有测试未破坏。
- BACKLOG 范围修订：fuzz-lite 移出 T003（用户指示），wire 编解码/CRC 校验集成归 T004；T004 转 Ready。
- 任务档案：[T003](tasks/T003-modbus-rtu-frame-model.md)