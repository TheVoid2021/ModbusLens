# Devlog — 2026-09-06（T004 Part B: Function 0x03 Codec Implementation）

- 设计修正（实现前）：①byteCount 合法范围收窄为 2~250 且偶数——quantity 1~125 ⇒ byteCount≥2，**byteCount=0 单帧即非法**，由 Part B 直接判 InvalidByteCount（B10b 锁定），不再推迟 T007（旧口径追加批注保留）；②澄清 byteCount **不是 RTU serial frame delimiter**（帧定界属 T010）。
- TDD RED：`Function03.h` 仅声明 + `tests/test_function03.cpp`（B01~B12，b10 拆 a/b）+ target `modbuslens_function03_tests` → 链接失败，15 处 undefined reference（configure/compile 均通过）。stub 未写、未提交。
- 实现 `Function03.{h,cpp}`：三个语义模型（defaulted `==`）+ `Function03DecodeErrorCode` 五值 + 三个 decoder（校验顺序：功能码→长度→内容规则）+ 匿名 namespace 的 `readBigEndianUint16(high, low)`；response 校验 byteCount∈[2,250] 偶数且 `data.size()==1+byteCount`。
- GREEN：B01~B12 全过（15 passed, 0 failed, 2ms）；全项目 ctest 5/5（smoke/crc/frame/codec/f03）；clean 全量重建 30 targets 零警告。
- 归档：T004 档案补齐并标 **DONE**（含 12 题必答、byteCount 修正留痕）；PROJECT_STATUS（Current Task=None、Last Completed=T004、M2 ✅ 关闭、LKGC 待回填）；BACKLOG（T004 Done、M2 完成、T005 Ready）；ARCHITECTURE/TEST_STRATEGY/INTERVIEW 同步；AGENTS.md 追加 Git policy（仅可 amend 当前任务最新未 push 提交、禁 rewrite 历史、当前不 push）。
- 提交：代码提交 `T004(Part B): implement Function 0x03 semantic decoder`（= 新 LKGC）+ docs-only 回填提交。
- 任务档案：[T004](tasks/T004-modbus-rtu-codec.md)