# Devlog — 2026-09-05（T004 Learning: RTU Codec 学习与范围细化）

- 建立 T004 档案：Codec 概念（Encoder/Decoder）、为什么 transport 不该自带协议编码、Part A（Frame↔wire bytes，含 CRC 序列化/验证）与 Part B（0x03 data 字段解释）的数据流与边界。
- 记录 0x03 三种帧字段：request（startAddress/quantity，16-bit 大端）、normal response（byteCount + 寄存器值，2 寄存器×2B=4B）、exception（0x83 = 0x03|0x80，结构化保存 exceptionCode）。
- 双字节序规则辨析：寄存器字段高字节在前 vs CRC 低字节在前（V1.02），概念公式 `(high<<8)|low`；禁 memcpy uint16（主机端序隐式决定协议顺序）。
- CRC 失败路径确认：不产生可信 Frame、raw bytes 留给诊断。
- BACKLOG：T004 更名 Modbus RTU Codec、拆 Part A/B、确认 fuzz/benchmark 不在范围；T005 不变。PROJECT_STATUS 四段式同步。
- 本阶段 docs-only：src/、tests/、CMakeLists.txt 未改动；LKGC 保持 `a44a6d2`。
- 任务档案：[T004](tasks/T004-modbus-rtu-codec.md)