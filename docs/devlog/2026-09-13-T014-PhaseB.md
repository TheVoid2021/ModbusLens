# Devlog — 2026-09-13 T014 Phase B Implementation

> 简记 + 链接（详见 [T014 档案](../tasks/T014-diagnostic-detail-preservation.md)）。

- **T014 Phase A Review = PASS**（用户），三项 refinement（production invariant 的防御边界 / token 与文案分层 / 不建 issue 统计子系统）写入档案 §28.5。
- **Implement（Test First）**：A `0ec6e06` 模型表面 + RED 测试（tx **11 passed / 9 failed**，9 条 `issue.has_value()==FALSE` 断言——真实断言级 RED）；B `900131d` `makeProtocolError`/`makeIssue` 接线七分支（tx **20/20**、ctest **23/23**）；C `cc8393a` prompt（`issue=<token>` + 载荷 + system 语义句族）/agent tools（detail 全量、anomalies 简化、hasX）/UI（issueText role + ProtocolError 行 secondary text）传播。
- **边界兑现**：六状态零改动；Statistics 逐位不变（STAT-B09）；Replay/Serial 零逻辑改动（同漏斗）；Baseline finding 不变；batch issue summary = DEFERRED（本轮无消费者）；agent 三工具/白名单/read-only 不变；`samples/demo_v2.mlog` 未进测试、未跟踪、未改。
- **验证**：clean 147 targets 零警告；ctest 23/23；qml smoke exit=0；deploy + minimal-PATH exit=0；零真实 ModelScope 调用。Manual UI Smoke = WAITING FOR USER。
- **LKGC candidate `cc8393a`（待 Review）**；verified LKGC 仍 `99f17d6`。未 push。
- 教训速记：聚合源兼容 ≠ 零警告（-Wmissing-field-initializers 两波，value-init 工厂根治）；RED 用断言失败需要“类型壳先行、行为后接”的两提交次序。

## Final Acceptance（同日晚补记）

- **用户 Manual UI Review = PASS**：A. Demo regression（四行/Dashboard/行高/布局）正常；B. 临时非仓库文件 `t014_protocol_error.mlog` 下 ProtocolError 行正确显示第二行 `响应地址不匹配（请求 0x01 / 响应 0x02）`——readable、行高扩展正确、无重叠/裁剪、列对齐、仅确定性措辞、无推测 root-cause。
- **T014 = DONE；Phase A/B = DONE**；**verified LKGC 推进 `99f17d6` → `cc8393a`**（docs-only archive `213bba5` 不作为 LKGC）。
- 措辞订正入档：生产文件 9 个（非 7）；不变量为 production 单向 `ProtocolError ⇒ issue`（downstream 对手工构造 issue=nullopt 防御 omit，不写无条件 iff）。