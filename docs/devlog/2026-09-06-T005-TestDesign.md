# Devlog — 2026-09-06（T005 Learning: Simulator 范围收缩与测试设计）

- 范围收缩：T005 只做最小 Slave 端点（单设备地址 + vector 连续 Holding Registers + 0x03 正常响应 / 0x02 越界异常 + 0x01/0x03 malformed 异常）；IFrameSource / VirtualMaster / 轮询 / 虚拟时钟 / seed 全部推迟（单数据源阶段不过早抽象，等 Replay/Serial 出现真实共性再提取）；Timeout / CRC fault / delay 归 T006。
- `SimulatedSlave` 定案：`handleRequest(Frame) -> variant<ModbusRtuFrame, IgnoredRequest>`——协议级结果一律用 Frame 表达（含异常响应），唯一非 Frame 结果是地址不匹配；v1 无 SimulatorError、无 broadcast。
- 四条流程定案：正常请求（复用 T004B decoder + 越界检查 32 位防溢出 + 大端写侧组包）/ 起点越界 / 区间跨末尾 / malformed（decoder 错误映射 0x01/0x03 异常码，不静默吞帧）。
- 测试矩阵 SIM-T01~T07（P0×7/P1×1，T05 专测区间跨末尾、T06 专测地址归属、T07 专测复用 decoder 与 malformed 映射）+ SIM-I01 全链路闭环（T002→T005 首次通电，P0）。
- 12 题问答与实施计划落库；本阶段 docs-only，src/、tests/、CMakeLists.txt 未改动，LKGC 保持 `e8b62f6`。
- 任务档案：[T005](tasks/T005-simulator-basic-slave.md)