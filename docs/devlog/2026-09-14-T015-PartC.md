# Devlog — 2026-09-14 T015 Part C (Function 0x10 passive)

> 简记 + 链接（详见 [T015 档案](../tasks/T015-passive-replay-expansion.md)、[ADR-003](../adr/ADR-003-broadcast-outcome-semantics.md)）。

- **RED（`92ccf16`）**：requestIssues 有序 collection 迁移（单值 → `std::vector<TransactionRequestIssue>`，无 singular+plural 双事实源）+ Function16 structural reader/normal response decoder（**无 encoder**）+ 全部下游迁移；passive **42 passed / 12 failed**——12 条断言级 RED（Function16 旧链路 Unsupported ⇒ Success/matcher/broadcast 期望全破）。教训：RED 不得以 crash 形态出现（空 transactions 解引用 SIGSEGV → 补 size guard 后全部转为优雅断言失败）。
- **GREEN core（`e1c9e2c`）**：Function16 被动语义接线（normal matcher 恰 4 字节 + `WriteMultipleRegistersEchoMismatch` 复用 T015 既有载荷列）+ broadcast-capable 集 **{0x06, 0x10}**（正交：invalid broadcast + NO_RESPONSE = ExpectedNoResponse + requestIssues；任何 response bytes = UnexpectedResponseForBroadcast + requestIssues）→ passive **54/54**。
- **GREEN downstream（`96ed9e2`）**：Agent detail `request_issues` 数组（旧 singular 键删除）；Prompt 全部 issue 逐条 machine facts；Controller「；」连接；模型文案（合法范围 1–123 / 字节数 / 长度 / echo 四类）；QML 64px 两行 wrap 不裁剪。
- **ExpectedNoResponse 语义澄清落地**：TransactionAnalysis.h / DiagnosisPromptBuilder / ADR-003 三处“valid broadcast-capable”旧表述同步为正交口径（expectation/outcome 与 request validity 分离）；统计公式未动。
- **验证**：clean 153 targets **零警告**；ctest **24/24**；qml smoke exit=0；deploy + minimal-PATH exit=0；安全 grep 零命中（无 Function16 encoder/send/写工具，SerialTransactionSession 仍仅 FC03 read-only）；零真实 ModelScope；6 个 untracked sample 未用未动。
- **状态**：T015 Part C = IMPLEMENTED / AWAITING REVIEW；**LKGC candidate = `96ed9e2`**（Manual UI Smoke + 用户 Review 前不推进）；verified LKGC 仍 `02ce302`。未 push。