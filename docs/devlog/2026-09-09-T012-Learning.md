# Devlog 2026-09-09 — T012 Agent Tools: Learning / Test Design（docs-only）

## 今日工作

- **T012 正式启动（Learning / Test Design，docs-only）**：零 production/tests/CMake 改动，零真实 ModelScope 调用，零 quota。
- **仓库事实核验**：HEAD `2e2790d` / LKGC `01841b1` / working tree clean；代码层核验 DiagnosisContext/Report/TransactionStatistics/Controller（activeDiagnosisTransactions_、activeBatchRevision_、aiRequestGeneration_）/TransactionListModel/PromptBuilder/ModelScopeDiagnosisClient；确认当前**无持久 transaction id**（只有行序与 vector 同源同序）。
- **定案（写入 docs/tasks/T012-agent-tools.md TD-1~TD-11）**：
  - 三工具（v1 冻结）：get_session_summary / get_recent_anomalies（上限 20，与 T011 同政策）/ get_transaction_detail；全部只读 active batch 确定性事实，输出 typed struct → provider 层 JSON。
  - **transaction_id v1 contract**：当前 active batch 内 1-based 序号（与 row index 区分、与 T011 prompt 编号同构；batch 替换即失效，跨 batch 查询 v1 不支持——no memory 的显式边界）。
  - **dispatcher 定案**：方案 B（enum + explicit dispatcher），否决 registry（三工具无动态需求，这里是过度设计；面试可解释性 + 穷举测试是硬理由）。
  - **ModelScope Tool Calling contract 核验**：Qwen3 官方文档确认支持 function calling（OpenAI 兼容 tools/tool_calls shape、role=tool 回传、官方点名"模型可能不守协议，必须自行解析"、推荐 Hermes-style）；**但 ModelScope API-Inference 是否透传 tools 字段未被文档证实**（文档站 SPA，无法静态取证）→ 定为 **Implementation Part B 第一步 = 需用户授权的 Live Tool-Calling Probe（1 次最小配额）**，结论二选一（原生 tools 路径 / Hermes 文本协议路径），不猜协议。
  - **Agent loop**：FSM + max tool rounds = 3；超限 ToolRoundLimitExceeded，无自动 retry。
  - **验证/错误 contract**：UnknownTool / InvalidArguments / TransactionNotFound / MalformedToolCall / ToolRoundLimitExceeded（小 enum，provider 错误沿用 AiDiagnosisErrorCode）。
  - **async validity**：run 绑定 capturedRevision + 独立 agentRequestGeneration_（复用 T011 双层 guard 模式，不造第三套系统）；AI 与 Agent 互斥（共享 client 单飞）。
  - **UI scope**：Diagnosis pane 一行输入 + 提问/取消 + PlainText 回答；不做聊天 UI/历史/memory；T011 Ask AI 一行不改、一刻不删（fallback 保持）。
  - **拆分推荐**：Part A（Read-only Tool Layer，零网络）+ Part B（Agent Runtime + UI + probe 先行）——有实质风险隔离。
  - **测试矩阵**：AGENT-A01~A08（tool layer，6 个 P0）+ AGENT-B01~B14（runtime，8 个 P0，含 batch 切换×2、supersede、cancel、注入、零改动事实）。
  - **Demo 三问答**：异常概览 / 0x02 单条 / "帮我改串口重发"→ 权限边界展示。
- **新 ADR**：`docs/adr/ADR002-readonly-tool-agent-architecture.md`（D-A1 read-only contract / D-A2 typed deterministic tool results / D-A3 run binds activeBatchRevision；不重复 D5）。
- **面试问答**：15 问已写入任务档案要点。

## 状态

- 本轮 docs-only commit 不推进 LKGC（保持 `01841b1`）；未 push。
- T011 DONE / ISSUE-004/005/006 RESOLVED / T012 IN PROGRESS（Learning 完成、Implementation 待用户批准）/ M6 IN PROGRESS。
- 下一步（待批准）：Part A Implementation（离线 TDD）。

## 关联档案

- 任务：`docs/tasks/T012-agent-tools.md`；ADR：`docs/adr/ADR002-readonly-tool-agent-architecture.md`