# Devlog — 2026-09-05（T002 Phase C: Test First + Implementation）

- 建立 `modbuslens_core`（STATIC，不链任何 Qt）承载 `src/core/protocol/ModbusCrc.{h,cpp}`；`modbuslens` 与新测试 target `modbuslens_crc_tests` 依赖它。
- TDD RED：先建接口 + 6 个正式测试（T01–T06），以 `return 0x0000` stub（仅工作区、未提交）驱动构建，采集真实失败证据——5 个 KAT/边界用例 FAIL（Actual 0 vs Expected 0x4B37/0x0A84/0xFFFF/0x40BF/0xCA45），而 T06（zero-remainder）PASS：恒 0 stub 骗过 invariant、骗不过 KAT，实证 Phase B 证据分级论断。QtTest stdout 在 Git Bash 管道下丢失，改用 `-o build/red_crc.txt,txt` 落盘取证。
- 实现：按位算法（init 0xFFFF → 每字节 XOR → 8×(先取 LSB→右移→LSB=1 则 XOR 0xA001)），值语义位运算、与端序无关。
- GREEN：6 用例全过（8 passed, 0 failed, 2ms）；全项目 ctest 2/2（smoke + crc）；clean 全量重建零警告。
- 文档：T002 档案补齐全部章节（Implementation/Problems/Verification RED→GREEN/Result= DONE/Knowledge Learned 六问）；PROJECT_STATUS（Current Task=None、Last Completed=T002、Next=T003、LKGC 待回填）；BACKLOG T002 Done、T003 Ready；02_ARCHITECTURE（core target）/04_TEST_STRATEGY（已落地测试）/INTERVIEW_NOTES 同步；PhaseB devlog 追加 404 澄清（官方资源未必失效）。
- 提交：代码提交 `T002: implement CRC-16/MODBUS core with KAT tests`（= 新 LKGC）+ docs-only 回填提交（哈希写回档案与 STATUS）。
- 任务档案：[T002](tasks/T002-modbus-crc16.md)