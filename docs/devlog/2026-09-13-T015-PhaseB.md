# Devlog — 2026-09-13 T015 Phase B (Passive Core + FC06 + Broadcast)

> 简记 + 链接（详见 [T015 档案](../tasks/T015-passive-replay-expansion.md)、[ADR-003](../adr/ADR-003-broadcast-outcome-semantics.md)）。

- **Phase A Review = PASS**：Gate A~F 全批；Gate C 批准**第七状态 `ExpectedNoResponse`** 与统计公式（completed 含广播；rate=success/(completed−expectedNoResponse)；分母 0 ⇒ nullopt）。口径订正（S7/S8 属 T007 时代覆盖；echo mismatch 需独立 issue）入档；官方协议事实回填 `03_MODBUS_LEARNING.md §4.5`（FC06 echo/0x86、0x10 1..123 与 byteCount、generic exception、broadcast 仅写类）。
- **RED（`477ed44`）**：模型表面先行 + 21 个 passive 测试 → **8 passed / 13 failed**，13 条全是断言失败（旧整批失败链路把历史事实当执行错误）。用户要求的五类行为（FC06 / generic FC08 Exception / S6 / broadcast / per-record continuation）全部在旧代码上真实 FAIL。
- **GREEN core（`2ba719f`）**：`PassiveTransactionAnalysis`（request 分类单点 / generic exception 单点 / FC03 复用 T007 / broadcast=addr0∧FC06 / unsupported 显式事实）+ `Function06`（无 encoder）+ `TransactionRequestIssue` + per-record Replay（analyzed + unsupportedRecords）+ 统计公式 + Baseline（`ExpectedNoResponseObserved` + Healthy 四条件）→ ctest 24/24。
- **GREEN downstream（`6944fd5`）**：dashboard「预期无响应」卡 + replay 非致命提示条 + request-issue 第二行文案；Prompt（`expected_no_response` / `request_issue` / system 语义句族）；Agent（summary `expected_no_response`、detail `request_issue_code`/`response_expected=false`、anomalies 排除新状态）；测试 AI-B20 / AGENT-A12 / UI-T02·T03 / DIAG-A12·A13 / STAT-B10。
- **验证**：clean 152 targets **零警告**；ctest **24/24**；qml smoke exit=0；deploy + minimal-PATH exit=0；Active Serial 写权限零新增（`encodeWriteSingleRegister|Function16|writeRegister(` grep 零命中）；demo_v1/T014/Serial/Simulator 零回归。**零真实 ModelScope 调用**；`samples/demo_v2.mlog` 未进测试、未改、未跟踪。
- **边界**：**Function 0x10 normal semantics = NOT STARTED（Part C）**；request-wire corruption = Gate E Scope A deferred；FRAME_FRAGMENT/t1.5/t3.5/UART/register-map/per-device 全部未动。
- **LKGC candidate `6944fd5`**（Manual UI Review + 用户 Review 前不推进）；verified LKGC 仍 `cc8393a`。Manual UI Smoke = WAITING FOR USER。
## Semantic audit（用户 Review 前专项，同日补记）

- **Generic exception request-function MSB 边界**：matcher 原缺守卫——request.fc=0x88 时 `0x88|0x80==0x88` 自我匹配，1 字节载荷被误判为合法 Exception 0x01。
- RED：新增 PASSIVE-P16 在旧逻辑 FAIL（21 passed/1 failed，误接受）；修复：显式 `(request.functionCode & 0x80) == 0` 前置条件，0x88/0x88 落入既有 Unsupported 路径（零新状态、零新 issue code、未碰 Active Serial/0x10）。
- GREEN：passive 22/22、clean 152 零警告、ctest 24/24；合法路径对照断言（FC08+0x88/0x01 → Exception 0x01）保留。
- **LKGC candidate 刷新为 `02ce302`；`6944fd5` 作废。**

## Final Acceptance（用户 Review = PASS，同日补记）

- **Manual UI Smoke = PASS**（全项）：Demo 回归（四行 + 新「预期无响应」卡=0）；demo_v1 Replay 不变且无提示条；Broadcast 显示「预期无响应」、计数正确、成功率不被稀释、不显示 Success/Timeout、无“写入成功”措辞；unsupported 非致命提示且不计入统计；invalid FC03 + Exception 0x03 同时显示 quantity=126/max=125；FC06 echo mismatch 清单含（自动由 PASSIVE-P03/UI 文案层锁定）；布局无回归。
- **verified LKGC 推进 `cc8393a` → `02ce302`**；`6944fd5` **superseded**；docs-only 提交（`fdefb0e` 等）不作 LKGC。
- **T015 整体仍 IN PROGRESS**：Part C（Function 0x10 normal semantics）NOT STARTED。
- Bookkeeping 订正：以 Git `show --name-only` 并集为准——23 个 src 文件 + CMakeLists.txt = **24 tracked production/build paths**；**8 个测试文件**；此前“13 production files / 9 tests”为 component/file 计数混用，作废。
- Semantic audit 定性：**generic exception matcher 的 protocol-semantic edge-case bug**（不是 Provider/parser/device bug）。
