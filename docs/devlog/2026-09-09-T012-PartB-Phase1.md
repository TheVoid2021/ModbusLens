# Devlog 2026-09-09 — T012 Part B Phase 1: Native Agent Runtime（Implementation）

## 今日工作

- **T012 Part B Phase 1 实现完成（真实 RED→GREEN；code/test commit `da453a7`，LKGC candidate 待 review）**，全部经 localhost fake server 自动验证，零真实 ModelScope、零公网、零 quota：
  - **AgentRuntime**（`src/ui/agent/AgentRuntime.{h,cpp}`）：有界 FSM（Idle→WaitingForModel→ExecutingTools→Completed/Failed）；每 run = 用户问题 + immutable snapshot + runGeneration + capturedBatchRevision，无跨 run memory；**两个硬上限同时生效**——MAX_TOOL_ROUNDS=3 与 MAX_TOTAL_TOOL_CALLS=3（任何超限响应在执行前整体拒绝，绝无部分执行）；多 tool_calls 同一响应内先 parse/validate 全部再按 provider 顺序执行，逐 call role=tool + 原 tool_call_id；错误契约 InvalidQuestion（空/whitespace/>1000 拒绝不截断）/MalformedToolCall（非 object/坏 JSON/缺或重复 id/type≠function）/UnknownTool/InvalidArguments/TransactionNotFound/ToolRoundLimitExceeded/ToolCallLimitExceeded；provider 错误复用 AiDiagnosisErrorCode；双层 stale guard（capturedBatchRevision + currentAgentGeneration seam，静默丢弃迟到交付）；cancel 静默 + 迟到不写答案；同 batch 新 run 显式 supersede 旧 run。
  - **ModelScopeAgentClient**（`src/ui/agent/ModelScopeAgentClient.{h,cpp}`）：native tool-calling round 客户端——整个 assistant message object 返回（tool_calls 在其中），reasoning_content 永不当答案；完整复刻 ISSUE-005 安全契约（单一 QTimer owner/abort-reason 分类/同源重定向/TLS 验证/不定日志 token）；共享 T011 的 ModelScopeClientConfig/AiAbortReason/AiDiagnosisErrorCode（值类型），未改 T011 client 一行。
  - **AgentPromptBuilder**（独立 system instruction：read-only authority/tool 权威/evidence scope/异常独立/输出中文 PlainText 约束 + 固定三工具 schema，additionalProperties=false）；T011 DiagnosisPromptBuilder 零改动。
  - **Part A 增量（语义不变）**：`validateAgentToolCall`（validate-only 孪生，同规则）+ `makeAgentToolContext`（P0 self-consistent snapshot builder：statistics 一律从同一份 copied transactions 经 summarizeTransactions 重算）。
  - **fake Chat Completions server 扩展**：多回合 FIFO 脚本队列（enqueueResponse/clearScript；setNextResponse 单发行为保持兼容）+ 全请求捕获列表。
- **测试**：AGENT-B01~B18 + Part A 新增 a10（snapshot 自洽）；**RED = 47 处 undefined reference**（先测试后实现）；GREEN = 全部通过。
- **自动验证**：clean 全量重建 **142 targets 零警告**；ctest **22/22**；测试使用 127.0.0.1 fake server 与 fake token，零真实调用。
- **实现中修复的问题**：测试 Harness 忘记 `server.start()` → 全部网络用例打向未监听端口（NetworkError/requestCount=0）——根因定位后一行修复，全部转绿（真实教训：requestCount==0 是"连接根本没发生"的信号，不是协议解析问题）。

## 状态

- T012 Part A = DONE；**Part B = IN PROGRESS；Part B Phase 1 = IMPLEMENTED / AWAITING REVIEW（`da453a7`）；Phase 2 = NOT STARTED**；M6 = IN PROGRESS。
- verified LKGC 仍 **`797269a`**（Phase 1 candidate 待用户 review 后推进）。
- 未开始 QML Agent UI / Controller 集成 / 真实 Live Agent Demo；未 push。

## 关联档案

- 任务：[T012-agent-tools](../../tasks/T012-agent-tools.md)（Phase 1 段追加）；ADR：[ADR002](../../adr/ADR002-readonly-tool-agent-architecture.md)。