# Devlog — 2026-09-06（T006 Learning: 确定性故障注入学习与测试设计）

- 范围收缩：T006 只做确定性四模式（None/DropResponse/CorruptCrc/ArtificialDelay）；random fault/probability/seed/real sleep/QTimer/QThread/timeout timer/丢包概率/burst/noise 全部移出（故障由测试或用户显式选择——确定性、测试稳定、Demo 可复现、防过度设计）。
- 概念澄清落库：①Timeout 不是报文而是 Session 层的判断——T006 只交付 DropResponse，"无响应 + 等待阈值 → Timeout"归 T007；②CRC fault 只作用 encoded wire bytes（SimulatedSlave 始终生成正确 Frame 且不被修改；Frame 不存 CRC）；③ArtificialDelay 只是元数据（不真 sleep），未来 Runtime 消费。
- 数据模型定案：`applySimulationFault(span, config) -> variant<DeliveredWire{bytes,delay}, DroppedResponse>`；CorruptCrc 固定策略 = 末 CRC 字节 XOR 0x01（payload 不动，示例 BA 7A→7B，decode 必得 CrcMismatch）。
- 三层区分表落库：Exception Response（设备真回复、CRC 正确）/ CRC Error（正确响应被线路损坏）/ DropResponse（没交付）。
- 测试矩阵 FAULT-T01~T05（P0×4/P1×1，T05=确定性护栏）+ I01（Slave→CRC fault→decoder，P0）/ I02（Drop，P1）落库；ISSUE-001 已在 INTERVIEW_NOTES 有索引（T005 加入，无需重复）。
- 本阶段 docs-only，src/、tests/、CMakeLists.txt 未改动，LKGC 保持 `3a896df`。
- 任务档案：[T006](tasks/T006-fault-injection.md)