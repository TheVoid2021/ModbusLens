# Devlog — 2026-09-06（T008.1: Windows Standalone Deployment Fix）

- 根因已确认（ISSUE-002）：Explorer 全局 PATH 中 Git/8.1 旧 MinGW runtime 抢占 Qt 13.1。
- 修复：`scripts/deploy_windows.bat`——从 `build/debug/CMakeCache.txt` 自动解析编译器 bin 与 Qt bin（无硬编码路径），创建干净 `build/deploy/`：复制 exe → windeployqt（--qmldir src/ui/qml + --compiler-runtime）→ **强制从编译器 bin 覆盖三件套**（确定性 provenance）→ 部署应用 QML 模块（ModbusLens/qmldir + Main.qml，修复"类型 Main 不在模块中"）→ 关键文件校验。
- **Runtime Provenance = VERIFIED**：deploy 三件套 SHA256 与编译器 bin 完全一致（windeployqt 部署的本就正确，脚本按规则仍强制覆盖复核）。
- **Minimal-PATH smoke = PASS**：`PATH=C:\Windows\System32;C:\Windows` 下 `--qml-smoke-test` exit=0——纯部署目录依赖启动，无 Qt/MinGW/Git/Anaconda PATH 污染。
- **普通运行存活 = PASS**：minimal PATH 下无 smoke 参数启动，进程存活确认。
- **脚本可重复生成验证 = PASS**：清空 build/deploy → 重跑脚本 → 44 项 + 应用模块目录 → 再验 smoke 通过。
- 发现与修复两个脚本 bug：①Qt6_DIR 路径推导正反斜杠字面量不匹配（`/lib/cmake/Qt6` vs `\lib\cmake\Qt6`）；②`start ""` 空 title 被 MSYS 转为 `\\`（改用启动器 bat 绕过）。
- clean build 86 targets 零警告 + ctest 14/14（deploy 目录生成前完成复验）；SimulatedSlave/业务代码零修改。
- ISSUE-002 状态 = 修复已部署并验证（条件 1–4 满足）；**条件 5（用户 Explorer 双击确认）= 待用户执行**，确认后置 RESOLVED。
- 提交：代码/脚本提交 `T008.1: add standalone Windows deployment workflow`（= 新 LKGC）+ docs-only 回填提交。
- Issue：[ISSUE-002](issues/ISSUE-002-explorer-launch-dll-collision.md)；脚本：[deploy_windows.bat](../../scripts/deploy_windows.bat)