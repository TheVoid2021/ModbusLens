# Devlog — 2026-09-06（T004 Part A: RTU Wire Codec Implementation）

- TDD RED（真实 linker error）：`ModbusRtuCodec.h` 仅声明、`tests/test_modbus_rtu_codec.cpp`（A01~A07）+ target `modbuslens_codec_tests` 就位后构建——configure PASS、compile PASS、link FAILED，10 处 `undefined reference to modbuslens::core::encodeRtuFrame/decodeRtuFrame`。stub 未写、未提交。
- 实现 `src/core/protocol/ModbusRtuCodec.cpp`：encode（reserve 后依次 push address/function/data，`calculateModbusCrc` 现算，显式 low→high 追加）；decode（`size()<4 → FrameTooShort`；`first(size-2)` 分离 payload；`(unsigned)crcHigh<<8 | crcLow` 显式还原 `84 0A→0x0A84`；不一致 → `CrcMismatch` 不产 Frame；成功则从 span 子视图构造 Frame，不解释内容）。vector↔span 隐式转换零样板，未遇构造问题。
- 加入 `modbuslens_core`（保持零 Qt）+ 新 target `modbuslens_codec_tests`。
- GREEN：A01~A07 全过（9 passed, 0 failed, 3ms）；全项目 ctest 4/4（smoke/crc/frame/codec）；clean 全量重建 25 targets 零警告，ctest 复验 4/4。
- 文档：T004 档案补齐（Implementation/RED→GREEN/十问索引/Interview 增补）；PROJECT_STATUS（Part A DONE、LKGC 待回填、4/4）；BACKLOG/ARCHITECTURE/TEST_STRATEGY/INTERVIEW_NOTES 同步。
- 提交：代码提交 `T004(Part A): implement RTU wire codec with CRC validation`（= 新 LKGC）+ docs-only 回填提交。
- 任务档案：[T004](tasks/T004-modbus-rtu-codec.md)